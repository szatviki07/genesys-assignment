# Genesys Assignment

This project implements a solution for generating all possible letter combinations from a digit string (2–9), based on a telephone keypad mapping.

## Features

Handles input constraints (length ≤ 4, digits 2–9)
Returns combinations in sorted order
Includes unit tests

## Run tests

python -m unittest unit_tests_for_task.py
